name: clientServerC
run-name: ${{ github.actor }} exercice OS
on: [push]
jobs:
    check-bats-version:
        runs-on: ubuntu-latest
        steps:
            -uses: actions/checkout@v3
            -run: gcc server_prev.c -o server
            -run: gcc client_prev.c -o client
